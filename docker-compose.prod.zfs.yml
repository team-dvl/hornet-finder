# Override to use external volumes for PROD (pointant vers les vraies données)
volumes:
  api-db:
    external: true
    name: hornet-finder-api-db-18

  keycloak-db:
    external: true
    name: hornet-finder-keycloak-db-18
  
  frontend-dist:
    external: true
    name: hornet-finder-frontend-dist

